# 1. 모듈이란?
<details>
  <summary>(1) 모듈이란?</summary>

  #### 독립적인 코드 단위
  ```txt
  모듈 = 재사용 가능한 독립적인 코드 파일

  특징:
   - 하나의 파일 = 하나의 모듈
   - 독립적인 스코프 (다른 파일과 변수 이름이 충돌하지 않음)
   - export로 내보내고, import로 가져와서 사용
  ```
  
  #### 왜 모듈이 필요한가?
  ```txt
  문제:
   - 코드가 길어지면 원하는 부분을 찾기 어려움
   - 같은 코드를 여러 곳에 복사/붙여넣기 해야함
   - 함수 이름이 겹치면 예상치 못한 버그가 생김
   - 팀원과 같은 파일을 동시에 수정하면 충돌남

  해결:
   - 코드를 기능별로 파일에 분리
   - 필요한 파일만 import해서 재사용
   - 각 파일이 독립적인 스코프를 가짐
   - 누가 어떤 파일을 담당하는지 명확함
  ```
</details>

<details>
  <summary>(2) 모듈 없이 작업하기(문제점)</summary>

  #### 하나의 파일에 모든 코드
  ```HTML
  <!-- index.html -->
  <!DOCTYPE html>
  <html>
  <body>
      <script>
          // 🚨 모든 코드가 한 파일에 뒤섞여 있음

          // 날짜 관련
          function formatDate(date) {
              return date.toLocaleDateString('ko-KR');
          }

          // 유저 관련
          function getUser() {
              return { name: '홍길동', age: 20 };
          }

          // 화면 관련
          function showUser(user) {
              document.body.innerHTML = user.name;
          }

          // 메인 실행
          const user = getUser();
          showUser(user);

          // ... 이런 함수가 100개가 되면? 😱
      </script>
  </body>
  </html>
  ```
  #### 문제점:
  ```txt
  1. 코드가 길어지면 원하는 함수를 찾기가 힘듦
  2. 다른 프로젝트에서 쓰려면 복사/붙여넣기 해야함
  3. 함수 이름이 겹치면 예상치 못한 버그가 발생함
  4. 팀원이 같은 파일을 동시에 수정하면 충돌남
  ```
</details>

<details>
  <summary>(3) 모듈로 분리하기(해결)</summary>

  #### 파일 구조:
  ```txt
  project/
  ├── index.html
  ├── js/
  │   ├── utils/
  │   │   └── date.js        # 날짜 관련 함수
  │   ├── api/
  │   │   └── users.js       # 유저 관련 함수
  │   ├── ui/
  │   │   └── render.js      # 화면 관련 함수
  │   └── main.js            # 메인 실행 파일
  ```
  * 각 파일은 자기 역할만 함. 서로 필요한 게 있으면 import로 요청하면 됨
  
  #### 각 파일이 독립적:
  ```JavaScript
  // utils/date.js
  export function formatDate(date) {
    return date.toLocaleDateString('ko-KR');
  }

  // api/users.js
  export function getUser() {
    return { name: '홍길동', age: 20 };
  }

  // ui/render.js
  export function showUser(user) {
    document.body.innerHTML = user.name;
  }

  // main.js - 필요한 것만 가져와서 사용
  import { getUser } from './api/users.js';
  import { showUser } from './ui/render.js';

  const user = getUser();
  showUser(user);
  ```
  
  #### 장점:
  ```txt
  1. 기능별로 파일이 나뉘어 있어 원하는 코드를 찾기 쉬움
  2. 필요한 모듈만 import해서 다른 프로젝트에도 바로 재사용할 수 있음
  3. 각 파일이 독립적이라 이름이 겹쳐도 충돌이 없음
  4. 팀원마다 다른 파일을 맡아서 작업하면 충돌이 최소화됨
  ```
</details>

<details>
  <summary>(4) 브라우저에서 모듈 사용</summary>

  #### type="module" 속성:
  ```HTML
  <!DOCTYPE html>
  <html>
  <head>
      <title>모듈 예제</title>
  </head>
  <body>
      <!-- 일반 스크립트 (구식) -->
      <script src="script.js"></script>

      <!-- 모듈 스크립트 (최신) -->
      <script type="module" src="main.js"></script>
  </body>
  </html>
  ```
  * `type="module"`을 붙여야 브라우저가 이 파일을 모듈로 인식함
  * 이걸 빠뜨리면 import/export가 동작하지 않음
  
  #### 일반 스크립트 vs 모듈 스크립트
  ```txt
  일반 스크립트 (type 없음):
   - 전역 스코프 -> 변수 이름 충돌 위험
   - import/export 사용 불가

  모듈 스크립트 (type="module"):
   - 모듈 스코프 -> 각 파일이 독립적
   - import/export 사용 가능
   - strict mode 자동 적용 (실수를 잡아주는 엄격 모드)
  ```
</details>

<br>

# 2. import와 export
<details>
  <summary>(1) export 기본</summary>

  * export = 다른 파일에서 가져다 쓸 수 있도록 **내보내기** 하는 것
  #### 선언하면서 바로 export:
  ```JavaScript
  // math.js

  // 함수 export
  export function add(a, b) {
      return a + b;
  }

  export function subtract(a, b) {
      return a - b;
  }

  // 변수 export
  export const PI = 3.14159;
  ```
  
  #### 만들고 나서 한 번에 export:
  ```JavaScript
  // math.js

  function add(a, b) {
      return a + b;
  }

  function subtract(a, b) {
      return a - b;
  }

  const PI = 3.14159;

  // 마지막에 한꺼번에 내보내기
  export { add, subtract, PI };
  ```
</details>

<details>
  <summary>(2) import 기본</summary>

  * import = 다른 파일에서 export한 것을 **가져오기** 하는 것

  #### 필요한 것만 골라서 import:
  ```JavaScript
  // main.js

  // 하나만 가져오기
  import { add } from './math.js';

  console.log(add(2, 3)); // 5

  // 여러 개 가져오기
  import { add, subtract, PI } from './math.js';

  console.log(add(5, 3));      // 8
  console.log(subtract(5, 3)); // 2
  console.log(PI);             // 3.14159
  ```
  
  #### 전부 한꺼번에 import:
  ```JavaScript
  // main.js

  // math.js에서 export한 걸 전부 math 객체에 담기
  import * as math from './math.js';

  console.log(math.add(2, 3));      // 5
  console.log(math.subtract(5, 2)); // 3
  console.log(math.PI(;             // 3.14159
  ```
</details>

<details>
  <summary>(3) 별칭(alias) 사용</summary>

  #### import 시 별칭 붙이기:
  ```JavaScript
  // main.js

  // add가 두 파일에 모두 있어서 충돌
  // -> as로 별명을 붙여서 구분!
  import { add as mathAdd } from './math.js';
  import { add as stringAdd } from './string.js';

  console.log(mathAdd(2, 3));        // 5
  console.log(stringAdd('a', 'b'));  // 'ab'
  ```
  
  #### export 시 별칭 붙이기:
  ```JavaScript
  // utils.js
  function validate(email) {
      return email.includes('@');
  }

  // 내보낼 때 더 명확한 이름으로 바꿔서 export
  export { validate as validateEmail };
  ```
  ```JavaScript
  // main.js
  import { validateEmail } from './utils.js';

  console.log(validateEmail('test@gmail.com'));  // true
  ```
</details>

<details>
  <summary>(4) 실전 예제</summary>

  #### 파일 구조:
  ```txt
  project/
  ├── index.html
  ├── js/
  │   ├── utils/
  │   │   └── string.js   # 문자열 관련 함수
  │   ├── data/
  │   │   └── users.js    # 유저 데이터
  │   └── main.js         # 메인 실행 파일
  ```
  
  #### 유틸리티 모듈(utils/string.js):
  ```JavaScript
  // utils/string.js

  // 첫 글자를 대문자로 바꾸는 함수
  export function capitalize(str) {
      return str.charAt(0).toUpperCase() + str.slice(1);
  }

  // 글자 수가 넘으면 잘라서 '...' 붙이는 함수
  export function truncate(str, length) {
      if (str.length > length) {
          return str.slice(0, length) +
  '...';
    }
    return str;
  }
  ```

  #### 데이터 모듈(data/users.js):
  ```JavaScript
  // data/users.js

  export const users = [
      { name: 'hong gilldong', email: 'hong@example.com' },
      { name: 'kim cheolsu', email: 'kim@example.com' },
      { name: 'lee younghee', email: 'lee@example.com' },
  ];
  ```

  #### 메인 파일(main.js):
  ```JavaScript
  // main.js
  import { capitalize, truncate } from './utils/string.js';
  import { users } from './data/users.js';

  // 유저 목록 출력
  users.forEach(user => {
      const name = capitalize(user.name);   // 'Hong gildong'
      const email = truncate(user.email, 10);  // 'hong@exam...'

      console.log(`${name} - ${email}`);
  });
  ```
</details>

<br>

# 3. default export
<details>
  <summary>(1) default export란?</summary>
</details>

<details>
  <summary>(2) default export의 가장 큰 특징: 이름을 자유롭게!</summary>
</details>

<details>
  <summary>(3) 다양한 것을 default export 할 수 있어요</summary>
</details>

<details>
  <summary>(4) default + named export 혼합</summary>
</details>

<details>
  <summary>(5) 정리: 언제 뭘 쓸까?</summary>
</details>

<br>

# 4. named export vs default export
<details>
  <summary>(1) 한눈에 비교</summary>
</details>

<details>
  <summary>(2) 언제 무엇을 사용할까?</summary>
</details>

<details>
  <summary>(3) 실전 예제: 혼합 사용</summary>
</details>

<details>
  <summary>(4) 최종 정리 치트시</summary>
</details>

<br>

# 5. ES 모듈과 CommonJS 모듈
<details>
  <summary>(1) 왜 모듈 시스템이 두 개일까?</summary>
</details>

<details>
  <summary>(2) 문법 비교: "같은 일, 다른 모양"</summary>
</details>

<details>
  <summary>(3) 중요한 차이 하나: import 위치 제한</summary>
</details>

<details>
  <summary>(4) Node.js에서 ES 모듈 사용하기</summary>
</details>

<details>
  <summary>(5) 실전에서 만나는 상황</summary>
</details>

<details>
  <summary>(6) 정리</summary>
</details>
